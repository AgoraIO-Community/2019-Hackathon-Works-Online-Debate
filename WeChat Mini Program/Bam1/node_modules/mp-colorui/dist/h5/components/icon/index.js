import Nerv from "nervjs";
import { Text } from '@tarojs/components';
import Taro from "@tarojs/taro-h5";
import { isNumber } from "../utils/index";
import { pxMap } from '../utils/model';
export default class ClIcon extends Taro.Component {
  render() {
    const iconName = this.props.iconName || '';
    let iconNameClass = `cuIcon-${this.props.iconName}`;
    if (this.props.other) {
      iconNameClass = iconName;
    }
    const sizeClassName = this.props.size || 'normal';
    const bgColorClassName = this.props.color ? `text-${this.props.color}` : '';
    return <Text className={`${bgColorClassName} ${iconNameClass}`} style={{
      fontSize: `${isNumber(sizeClassName) ? Taro.pxTransform(sizeClassName) : Taro.pxTransform(pxMap[sizeClassName])}`
    }} />;
  }
}
ClIcon.options = {
  addGlobalClass: true
};
ClIcon.defaultProps = {
  size: 'normal',
  bgColor: undefined,
  iconName: undefined
};