import Taro from "@tarojs/taro-h5";
import Nerv from "nervjs";
import { View } from '@tarojs/components';
export default class ClAnimation extends Taro.Component {
  render() {
    const props = this.props;

    const type = props.type;
    const reverse = props.reverse ? 'animation-reverse' : '';
    return <View className={`animation-${type} ${reverse}`} style={{ animationDelay: `${props.delay}s`, animationDuration: `${props.duration}s` }} onAnimationStart={e => {
      props.onAnimationStart && props.onAnimationStart(e);
    }} onAnimationEnd={e => {
      props.onAnimationEnd && props.onAnimationEnd(e);
    }}>
      {this.props.children}
    </View>;
  }

}
ClAnimation.options = {
  addGlobalClass: true
};
ClAnimation.defaultProps = {
  type: 'fade',
  reverse: false,
  delay: 0,
  duration: 0.5
};